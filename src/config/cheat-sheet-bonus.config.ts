/**
 * Shown at the top of every /shop/[slug] product page in the "Cheat Sheets" category.
 */
export const CHEAT_SHEET_BONUS = {
  eyebrow: "Added bonus",
  title: "A-to-Z visit flow on one screen",
  body: "Each sheet is laid out so you can run the appointment top to bottom — from first glance at the chart through treatment cues, documentation nudges, and room reset — without digging through a playbook between patients.",
} as const;
