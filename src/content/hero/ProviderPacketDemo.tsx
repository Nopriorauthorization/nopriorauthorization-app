import InteractiveProviderPacket from './InteractiveProviderPacket';

export default function ProviderPacketDemo() {
  return (
    <div className="min-h-screen bg-black">
      <InteractiveProviderPacket />
    </div>
  );
}